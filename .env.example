# Local LLM Configuration
# Set to your local LLM server (Ollama, LM Studio, etc.)
LLM_API_URL=http://localhost:11434/api
LLM_MODEL=mistral

# Screen Capture Settings
# Interval in milliseconds between screenshots
CAPTURE_INTERVAL=15000
# Screenshot quality (0.0 - 1.0)
CAPTURE_QUALITY=0.7

# Memory Configuration
# Maximum number of memory entries to keep
MAX_MEMORY_SIZE=1000
# Database will be stored at ~/.vibechat/memory.db

# Development
NODE_ENV=development
